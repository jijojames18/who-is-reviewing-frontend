const config = {
  restEndPoint: "http://localhost:8000",
  msg: {
    userList: "List of reviewing users : {USER_LIST}",
    noActiveReviewers: "No one is currently reviewing this pull request.",
    toggleMessage: "Are you reviewing this pull request?",
    toggleYes: "Yes",
    toggleNo: "No",
    fallbackMessage: "Navigate to an open pull request to enable extension",
  },
};

export default config;
